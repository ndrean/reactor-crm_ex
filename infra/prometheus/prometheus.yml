global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "crm_reactor"
    scrape_interval: 10s
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app:4000"]
        labels:
          app: "crm_reactor"
